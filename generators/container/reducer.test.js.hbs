import {{ camelCase name }}Reducer from '../{{ properCase name }}.reducer';

describe('{{ camelCase name }}Reducer', () => {
  test('returns the initial state', () => {
    expect({{ camelCase name }}Reducer(undefined, {})).toEqual({});
  });
});
